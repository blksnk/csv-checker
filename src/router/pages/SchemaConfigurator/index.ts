import { SchemaConfiguratorPage } from "./SchemaConfigurator.page";

/** Route config for validation rules (`/rules`). */
export const SchemaConfigurator = {
  path: "/rules",
  displayName: "2. Define rules",
  Component: SchemaConfiguratorPage,
};
