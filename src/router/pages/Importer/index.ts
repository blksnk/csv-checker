import { ImporterPage } from "./Importer.page";

/** Route config for CSV upload (`/upload`). */
export const Importer = {
  path: "/upload",
  Component: ImporterPage,
  displayName: "1. Upload",
};
