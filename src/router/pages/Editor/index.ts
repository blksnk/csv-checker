import { EditorPage } from "./Editor.page";

/** Route config for the data editor (`/editor`). */
export const Editor = {
  Component: EditorPage,
  path: "/editor",
  displayName: "3. Edit & fix",
};
